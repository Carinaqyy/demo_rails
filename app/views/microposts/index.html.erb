<p style="color: green"><%= notice %></p>

<h1>Microposts</h1>

<div id="microposts">
  <% @microposts.each do |micropost| %>
    <%= render micropost %>
    <p>
    <% if defined?(@user) %>
      <%= link_to "Show this micropost", user_micropost_path(@user, micropost) %>
    <% else %>
      <%= link_to "Show this micropost", micropost %>
    <% end %>
    </p>
  <% end %>
</div>

<%= link_to "New micropost", new_micropost_path %>
